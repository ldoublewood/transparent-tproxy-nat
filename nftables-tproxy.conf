#!/usr/sbin/nft -f
# =============================================================================
# nftables NAT 透明网关 + TPROXY  完整配置
# =============================================================================
# 场景: 本机是局域网内一台普通机器（非路由器），仅有一个网卡（如 wlo1）。
#       - LAN 内其他客户端将默认网关指向本机 IP
#       - 本机将客户端的 TCP/UDP 请求通过 TPROXY 重定向到本地 :17893 代理
#       - 未被 TPROXY 截获的流量（ICMP 等）通过 MASQUERADE 转发到互联网
#       - 本机自己的默认网关指向真正的路由器（如 192.168.1.1）
#
# 部署: /etc/nftables.conf   Ubuntu 22.04 | nftables ≥ 1.0.2
#
#        ┌──────────────────────────────────────────────────┐
#        │  ⚠ 先执行策略路由（必须，只在 nftables 生效前） │
#        │    bash setup-tproxy-route.sh                    │
#        └──────────────────────────────────────────────────┘
# =============================================================================

flush ruleset

# =============================================================================
# 用户变量 — 务必按实际环境修改
# =============================================================================
define LAN_IFACE     = wlo1        # 局域网网卡名称
define LAN_SUBNET    = 192.168.2.0/24  # 局域网子网 (含本机)
define TPROXY_PORT   = 17893       # TPROXY 监听端口
define TPROXY_MARK   = 1           # 防火墙标记 (需与策略路由 fwmark 一致)

# =============================================================================
# table inet nat_gateway
# 一张表承载全部功能: TPROXY 截获 + 过滤 + NAT 伪装
# =============================================================================
table inet nat_gateway {

    # -----------------------------------------------------------------
    # 1. TPROXY 截获链 (mangle 优先级，比路由决策更早)
    # -----------------------------------------------------------------
    chain tproxy_prerouting {
        type filter hook prerouting priority mangle; policy accept;

        # (a) 排除本机回环
        iif "lo" accept

        # (b) 只处理 LAN 网卡入站流量
        iif != $LAN_IFACE accept

        # (c) 排除发往局域网内部的流量 (不代理内网通信)
        ip daddr $LAN_SUBNET accept

        # (d) 排除广播/多播
        ip daddr 224.0.0.0/4 accept

        # (e) 排除发往本机自身的流量 (SSH / 管理流量不走代理)
        #     tproxy 需要本机 IP，可用 fib 或显式列出
        #     此处用 fib daddr type local 排除所有本机地址
        fib daddr type local accept

        # ==========================================================
        # TPROXY 核心规则
        #   将 TCP 流量重定向到本地代理端口，保留原始目标地址
        #   标记 fwmark 供策略路由将包投递到本地 lo
        # ==========================================================

        # TCP → TPROXY
        ip protocol tcp \
            tproxy ip to :$TPROXY_PORT \
            meta mark set $TPROXY_MARK \
            accept

        # UDP → TPROXY (如果你的代理支持 UDP，取消下行注释)
        # ip protocol udp \
        #     tproxy ip to :$TPROXY_PORT \
        #     meta mark set $TPROXY_MARK \
        #     accept

        # 未匹配的流量继续走正常路由/转发流程
    }

    # -----------------------------------------------------------------
    # 2. input 链 — 控制发往本机自身的流量
    # -----------------------------------------------------------------
    chain input {
        type filter hook input priority filter; policy drop;

        # 回环全部放行
        iif "lo" accept

        # 已建立/关联的连接放行
        ct state established,related accept

        # 允许 LAN 内客户端 SSH 到本机
        iif $LAN_IFACE ip saddr $LAN_SUBNET tcp dport 22 accept

        # 允许 LAN 内 ping 本机
        iif $LAN_IFACE ip saddr $LAN_SUBNET ip protocol icmp accept

        # 允许 DHCP 客户端续约 (如果本机是 DHCP 获取 IP)
        iif $LAN_IFACE udp dport 68 accept

        # 其余入站一律记录后丢弃
        log prefix "nft-input-drop: " flags all counter drop
    }

    # -----------------------------------------------------------------
    # 3. forward 链 — 控制穿过本机的流量 (LAN ↔ 互联网)
    # -----------------------------------------------------------------
    chain forward {
        type filter hook forward priority filter; policy drop;

        # 已建立/关联的连接放行 (返回流量)
        ct state established,related accept

        # 从 LAN 发往互联网的流量 (TCP 已被 TPROXY 截获,此处主要放行 UDP/ICMP)
        iif $LAN_IFACE oif $LAN_IFACE ip saddr $LAN_SUBNET accept

        # TPROXY 回复包: 代理发出的欺骗包 (src 为外部地址) 需要到达 LAN 客户端
        # 这些包从本地 lo 接口生成，出 wlo1，需穿过 forward 链
        iif "lo" oif $LAN_IFACE accept

        log prefix "nft-fwd-drop: " flags all counter drop
    }

    # -----------------------------------------------------------------
    # 4. output 链 — 控制本机自身发出的流量
    # -----------------------------------------------------------------
    chain output {
        type filter hook output priority filter; policy accept;

        # 默认允许本机所有出站
        # 如果需要对特定目标做限制，在此添加规则
    }

    # -----------------------------------------------------------------
    # 5. postrouting 链 — NAT 伪装 (MASQUERADE)
    # -----------------------------------------------------------------
    #  只有被"转发"的包会到达这里。
    #  TPROXY 截获的 TCP 包已在 prerouting 被 accept → 本地投递,不会到这里。
    #  代理程序主动发出的出站连接走 output,也不到这里。
    #  所以这里主要伪装: UDP DNS / ICMP 等未被 TPROXY 处理的转发流量。
    # -----------------------------------------------------------------
    chain postrouting {
        type nat hook postrouting priority srcnat; policy accept;

        # 将从 LAN 发出、经本机转发的流量做源地址伪装
        oif $LAN_IFACE ip saddr $LAN_SUBNET masquerade
    }
}

# =============================================================================
# 完整数据流示意 (TCP 为例)
# =============================================================================
#
#  LAN Client (192.168.1.100)
#       │ TCP SYN → internet-host:443
#       ▼
#  ┌───────────────────────────────────────────────────┐
#  │ wlo1 (192.168.1.50) 本机                          │
#  │                                                   │
#  │ [prerouting mangle]                               │
#  │   ├─ iif wlo1, ip daddr != LAN → 匹配 ✓           │
#  │   ├─ tproxy ip to :17893                          │
#  │   └─ meta mark set 1, accept  ← 包被本地投递      │
#  │                                                   │
#  │ [策略路由: fwmark 1 → table 100 → lo]              │
#  │                                                   │
#  │ [代理程序 :17893]                                  │
#  │   ├─ getsockopt(SO_ORIGINAL_DST) → internet-host  │
#  │   ├─ 主动连接 internet-host:443                    │
#  │   └─ 通过 TPROXY socket 回复客户端                │
#  │       (src IP = internet-host, 欺骗)               │
#  │                                                   │
#  │ [代理出站包 → output → postrouting → wlo1 → 互联网]│
#  │ [欺骗回复包 → output → forward → wlo1 → LAN Client]│
#  └───────────────────────────────────────────────────┘
#
# =============================================================================
# 完整数据流示意 (UDP/ICMP — 非 TPROXY)
# =============================================================================
#
#  LAN Client
#       │ UDP DNS → 8.8.8.8:53
#       ▼
#  ┌───────────────────────────────────────────────────┐
#  │ wlo1 (本机)                                       │
#  │ [prerouting mangle] → 无 TPROXY 匹配 → 继续       │
#  │ [路由决策] → 目标非本地 → forward 链               │
#  │ [forward] → iif wlo1 oif wlo1 LAN → accept        │
#  │ [postrouting] → MASQUERADE (src → 192.168.1.50)   │
#  │ [wlo1 发出] → ISP 路由器 → 8.8.8.8                │
#  │                                                   │
#  │ 返回: 8.8.8.8 → ISP 路由器 → 192.168.1.50        │
#  │ [conntrack 反向 MASQUERADE] → forward → LAN Client │
#  └───────────────────────────────────────────────────┘
# =============================================================================

# =============================================================================
# 部署步骤
# =============================================================================
# 1. 配置并执行策略路由:
#    bash setup-tproxy-route.sh
#
# 2. 加载 nftables 规则:
#    nft -f /etc/nftables.conf
#
# 3. 开机自启:
#    cp nftables-tproxy.conf /etc/nftables.conf
#    systemctl enable --now nftables
#    cp tproxy-route.service /etc/systemd/system/
#    systemctl enable --now tproxy-route
#
# 4. 代理程序 (以 Xray 为例) inbound 必须启用 tproxy:
#    {
#        "port": 17893,
#        "protocol": "dokodemo-door",
#        "settings": { "network": "tcp,udp", "followRedirect": true },
#        "streamSettings": { "sockopt": { "tproxy": "tproxy" } }
#    }
#
# 5. LAN 客户端配置:
#    将默认网关设为本机 IP (如 192.168.1.50)
#    curl -v http://httpbin.org/ip   # 验证
# =============================================================================
